{
  "name": "cineform-forge-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "Cineform Forge Monorepo — Animation engine, AI assistant, templates, and webapp.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint \"apps/**/*.{ts,tsx}\" \"packages/**/*.{ts,tsx}\"",
    "format": "prettier --write \"apps/**/*.{ts,tsx}\" \"packages/**/*.{ts,tsx}\" \"*.{js,json,md}\"",
    "typecheck": "turbo run typecheck",
    "build": "turbo run build",
    "test": "turbo run test"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "@types/jest": "^29.5.7",
    "@testing-library/react": "^14.1.3",
    "@testing-library/jest-dom": "^6.1.5",
    "@testing-library/user-event": "^14.5.2"
  }
}