import React from 'react';
import styles from './TimelineEditor.module.css';

export const TimelineEditor: React.FC = () => {
  return (
    <div className={styles.timelineEditor}>
      {/* Timeline, sequences, and keyframes will go here */}
    </div>
  );
};