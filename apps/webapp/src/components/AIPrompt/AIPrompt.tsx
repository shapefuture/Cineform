import React from 'react';
import styles from './AIPrompt.module.css';

export const AIPrompt: React.FC = () => {
  return (
    <form className={styles.aiPromptForm}>
      {/* Text prompt input and submit button */}
    </form>
  );
};